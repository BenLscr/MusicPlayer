<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="@dimen/music_margin_top_bottom"
    android:layout_marginBottom="@dimen/music_margin_top_bottom"
    android:orientation="horizontal">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <TextView
            android:id="@+id/title_list"
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:gravity="center"
            android:text="Title"
            android:textStyle="bold"
            android:textSize="18sp"
            android:textColor="@color/colorTitle"
            android:textAppearance="?attr/textAppearanceListItem" />

        <TextView
            android:id="@+id/artist_list"
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:gravity="center"
            android:text="Artist"
            android:textColor="@color/colorArtist"
            android:textAppearance="?attr/textAppearanceListItem" />

    </LinearLayout>

    <ImageButton
        android:id="@+id/play_pause_shrink"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="end"
        android:background="@drawable/background_play_shrink"
        android:src="@drawable/button_play_shrink"/>

</FrameLayout>
